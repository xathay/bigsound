[package]
name = "big-cross-ladspa"
description = "LADSPA wrapper around BigCross — stereo crossfeed for PipeWire filter-chain"
version.workspace = true
edition.workspace = true

[lib]
name = "big_cross"
crate-type = ["cdylib"]

[dependencies]
big-cross = { path = "../big-cross" }
